import styled from 'styled-components';

export const StyledProperties = styled.div`
	width: 250px;
	height: 100%;
	padding: 0.8em;
	overflow: auto;

	h2 {
		margin: 0;
		font-size: 0.8rem;
		font-weight: normal;
		color: var(--grey-700);
	}
`;
